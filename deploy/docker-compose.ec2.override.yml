# Override de producción para EC2.
# Uso (desde la raíz del repo):
#   docker compose -f docker-compose.yml -f deploy/docker-compose.ec2.override.yml up -d --build
#
# - Frontend en puerto 80 (HTTP público)
# - MongoDB y API no se publican en el host (solo red interna Docker)

services:
  mongodb:
    # El stack en EC2 no necesita publicar MongoDB al host.
    ports: []

  backend:
    # El backend no se publica al host; el frontend (nginx) lo consume por red Docker.
    ports: []

  frontend:
    # Publicar el frontend en HTTP (puerto 80 del host).
    ports:
      - "80:8000"
